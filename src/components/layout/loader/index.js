export const LoaderComponent = () => {
    return (
        <div className={`grid grid-cols-1 text-center m-auto`}>
            <p className={`animate-spin`}><i className=" bi bi-arrow-clockwise"></i></p>
        </div>
    )
}